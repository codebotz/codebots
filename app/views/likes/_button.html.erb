<% if usuario_signed_in? %>
		<% if current_usuario.like?(articulo) %>
			<%= button_to unlike_path(id: articulo.id), remote: true, method: :delete, :class => "btn btn-danger btn-sm" do %><i class="fa fa-thumbs-o-down"></i><% end %>
		<% else %>
			<%= button_to like_path(id: articulo.id), remote: true, :class => "btn btn-info btn-sm"  do %><i class="fa fa-thumbs-o-up"></i><% end %>
		<% end %>
<% else %>
	<%= button_to like_path(id: articulo.id), remote: true, disabled: true, :class => "btn btn-info btn-sm", title: 'Necesitas iniciar sesión', 'data-toggle' => 'tooltip', 'data-placement' => 'top' do %><i class="fa fa-thumbs-o-up"></i><% end %>
<% end %>